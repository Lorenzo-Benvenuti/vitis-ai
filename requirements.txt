# Runtime dependencies, mirroring the [packages] section of the Pipfile.
# Pipfile.lock stays the source of truth for fully reproducible installs;
# this file exists for anyone not using pipenv.
# torch is pulled in automatically as a dependency of torchvision.

fastapi==0.136.3
uvicorn==0.48.0
pydantic==2.13.4
python-multipart==0.0.30
Pillow==12.2.0
torchvision==0.27.0
tqdm==4.67.3
